# Используйте официальный образ Python в качестве базового
FROM python:3.9-slim

# Установим рабочую директорию в контейнере
WORKDIR /app

# Скопируйем содержимое текущей директории в контейнер в /app
COPY . /app

# Сделаем порт 80 доступным для внешнего мира
EXPOSE 80

# Запускаем time.py при запуске контейнера
CMD ["python", "time.py"]